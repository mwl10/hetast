#!/bin/bash
#SBATCH -N 1
#SBATCH -c 1
#SBATCH --gres=gpu:1
#SBATCH -p res-gpu-large
#SBATCH --qos=long-high-prio
#SBATCH --job-name=hetvae
#SBATCH -t 10:00:00

source /etc/profile
source activate $HOME/miniconda/envs/hetvae

python3 $HOME/hetast/src/train.py \
    --data-folder ztf_test_data \
    --device mps \
    --niters 10000 \
    --start-col 1 \
    --save-at 1000 \
    --bound-variance \
    --kl-annealing \
    --lr 0.001 \
    --print-at 10 \
    --frac 0.9